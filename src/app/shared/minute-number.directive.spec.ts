import { MinuteNumberDirective } from './minute-number.directive';

describe('MinuteNumberDirective', () => {
  it('should create an instance', () => {
    const directive = new MinuteNumberDirective();
    expect(directive).toBeTruthy();
  });
});
